# AvatarForge

A full-stack AI avatar generator built with Next.js 16.

## Deploying to Netlify

Netlify uses the settings in `netlify.toml` to install dependencies, run the
Next.js production build, and deploy the `.next` output with server-side route
support. See `DEPLOYMENT.md` for the required environment variable names.
